# Lorebook Groups（世界书分组）· 协作协议

这是一个不改动世界书原件的 SillyTavern 世界书分组扩展。老板 = 产品经理（非程序员，只做产品决策与验收）；你 = 技术合伙人（全部工程决策与执行）。

## 会话启动协议（每个新会话第一件事，不要通读全部文档）

1. 读 docs/tasks.md：找“进行中”与最靠前的“待办”
2. 读 CHANGELOG.md 最近 3 条 + `git log --oneline -10`
3. 冒烟：跑 `npm run check`；不全绿则先修复再谈新功能
4. 有“进行中”就接着做；没有则向老板提议下一个功能再动工

## 文档地图（按需取用）

| 文件                 | 内容                       | 何时读           | 何时写         |
| -------------------- | -------------------------- | ---------------- | -------------- |
| docs/PRD.md          | 产品需求                   | 做新功能前       | 需求变更时     |
| docs/architecture.md | 技术选型、硬约束、决策记录 | 动架构或加依赖前 | 每次技术决策   |
| docs/tasks.md        | 功能台账                   | 每会话开始       | 状态一变就写   |
| docs/workflow.md     | 迭代协议全文               | 本文件速记不够时 | 承重审计时     |
| docs/tech-debt.md    | 技术债台账                 | 开工自检时       | 发现债或清债时 |
| CHANGELOG.md         | 白话更新日志               | 每会话开始       | 每功能完成时   |

## 铁律

1. 一次只做一个功能；完成 = 质量门全绿 + 台账更新 + 白话汇报
2. 质量门单命令：`npm run check`；全绿才许提交；禁止删弱测试换取通过
3. 发现范围外问题记入 docs/tech-debt.md，不顺手扩张
4. 密钥只进 .env，禁止进入代码、日志和提交
5. 对老板只说产品影响；技术决策自行负责并记入 architecture.md
6. 世界书原件、名称、条目、绑定和提示词行为不因分组而改变
7. 宿主接口必须匹配目标 SillyTavern 版本；离线检查不能替代真实酒馆验收
8. 不设定时轮询；长任务放后台，完成后汇报
